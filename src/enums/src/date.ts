export enum DATE_FORMAT {
  YEAR = 'YYYY-MM-DD HH:mm:ss',
  DAY = 'DD [天] HH:mm:ss',
  HOUR = 'HH:mm:ss'
}

export enum TIMEZONE {
  DEFAULT = 'Asia/Shanghai',
  SHANGHAI = 'Asia/Shanghai'
}

export enum TIME_CATEGORY {
  DAY = 'day',
  HOUR = 'hour',
  MINUTE = 'minute',
  SECOND = 'second'
}
